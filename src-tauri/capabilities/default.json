{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability (store only). Use desktop capability on Windows.",
  "windows": ["main"],
  "permissions": [
    "store:default"
  ]
}
